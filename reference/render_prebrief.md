# Render a naming pre-brief as compact markdown

Render a naming pre-brief as compact markdown

## Usage

``` r
render_prebrief(x)
```

## Arguments

- x:

  An `nli_prebrief` from [`naming_prebrief()`](naming_prebrief.md).

## Value

The markdown string, invisibly; also printed.
