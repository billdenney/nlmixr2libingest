# Load the sidecar auto-answer policy table

Load the sidecar auto-answer policy table

## Usage

``` r
sidecar_policy(path = NULL)
```

## Arguments

- path:

  Policy YAML path; defaults to the table shipped with the package.

## Value

The parsed policy list (`policies`, `escalate_types`).
