# Render lookup results as compact markdown (for CLI / agent consumption)

Render lookup results as compact markdown (for CLI / agent consumption)

## Usage

``` r
render_lookup(df)
```

## Arguments

- df:

  A data frame from [`lookup_canonical()`](lookup_canonical.md).

## Value

The markdown string, invisibly; also printed.
