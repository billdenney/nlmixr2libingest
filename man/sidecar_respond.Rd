% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sidecar.R
\name{sidecar_respond}
\alias{sidecar_respond}
\title{Auto-answer a sidecar request when policy permits}
\usage{
sidecar_respond(
  request,
  response_file = NULL,
  policy = sidecar_policy(),
  apply = FALSE,
  log_file = NULL
)
}
\arguments{
\item{request}{A request file path, or a parsed request list.}

\item{response_file}{Where to write the response JSON when \code{apply = TRUE}.}

\item{policy}{A policy table from \code{\link[=sidecar_policy]{sidecar_policy()}}.}

\item{apply}{Actually write the response/log (default \code{FALSE} = dry run).}

\item{log_file}{Append an audit record here when an answer is written.}
}
\value{
The \code{nli_sidecar_decision} (with \code{request_file} and \code{applied}).
}
\description{
Matches a request against the policy table; if EVERY question is
auto-answerable, optionally writes the runner's response file (selecting only
options the agent was offered) and an audit-log line. Otherwise it writes
nothing and the request escalates to a human.
}
