% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{build_register_index}
\alias{build_register_index}
\title{Build (or rebuild) the canonical-name register index}
\usage{
build_register_index(
  db_path = register_db_path(),
  dir = registers_dir(),
  quiet = FALSE
)
}
\arguments{
\item{db_path}{DuckDB file to write. Defaults to \code{\link[=register_db_path]{register_db_path()}}.}

\item{dir}{Register directory. Defaults to \code{\link[=registers_dir]{registers_dir()}}.}

\item{quiet}{Suppress the FTS-unavailable warning.}
}
\value{
Invisibly, the parsed register data frame.
}
\description{
Parses the three \code{nlmixr2lib} registers into a DuckDB file with a full-text
index plus a source signature (size, mtime, md5) used for freshness checks.
If the DuckDB \code{fts} extension cannot be loaded (e.g. offline), the index is
still built and lookups fall back to token \code{LIKE} matching.
}
