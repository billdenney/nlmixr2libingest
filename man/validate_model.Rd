% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_model}
\alias{validate_model}
\title{Validate a model (and optionally its vignette) into one terse result}
\usage{
validate_model(
  model,
  paper = NULL,
  level = c("fast", "full"),
  pkg = NULL,
  vignette = NULL,
  tol = 0.05
)
}
\arguments{
\item{model}{An \code{rxUi}, a model function, or an \code{nlmixr2lib} model name.}

\item{paper}{Optional paper text or path; enables the source-trace stage.}

\item{level}{\code{"fast"} (parse + conventions + source-trace) or \code{"full"}
(adds R CMD check and vignette render).}

\item{pkg}{Package directory for the full-tier R CMD check.}

\item{vignette}{Optional path to the model's validation vignette to render.}

\item{tol}{Relative tolerance for source-trace numeric matches.}
}
\value{
An \code{nli_validation} object: \code{status} (\code{"success"}/\code{"issues"}),
\code{issues} (data frame), \code{counts}, and the \code{stages} run.
}
\description{
Runs the whole validation chain and returns a compact pass/fix object
instead of the verbose logs that otherwise re-cache on every turn of the
build-check-fix loop. The fast tier (parse, conventions, source-trace) is
pure R and meant for iteration; the full tier adds R CMD check and a vignette
render. Convention checking is delegated to
\code{nlmixr2lib::checkModelConventions()} -- this function never reimplements it.
}
