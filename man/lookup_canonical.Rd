% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{lookup_canonical}
\alias{lookup_canonical}
\title{Look up canonical names in the nlmixr2lib registers}
\usage{
lookup_canonical(
  term,
  kind = NULL,
  top_k = 5,
  full = FALSE,
  db_path = register_db_path(),
  dir = registers_dir()
)
}
\arguments{
\item{term}{Search term: a canonical name, an alias, or a free-text concept
(e.g. \code{"body weight"}).}

\item{kind}{Optionally restrict to \code{"covariate"}, \code{"compartment"}, or
\code{"parameter"}.}

\item{top_k}{Maximum rows to return.}

\item{full}{If \code{TRUE}, include the full \code{example_models} list and \code{text}.}

\item{db_path, dir}{Index location and register directory.}
}
\value{
A data frame of matching entries (0 rows if none match), ordered by
relevance.
}
\description{
Returns the handful of register entries most relevant to \code{term} without ever
loading the multi-hundred-thousand-token register files into context. Exact
name and alias matches are surfaced first, then full-text (BM25) or token
matches. The long \code{example_models} list is summarised to a count by default.
}
