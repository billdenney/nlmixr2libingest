% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/budget.R
\name{run_token_stats}
\alias{run_token_stats}
\title{Token / cost analytics over the runner's RunRecords}
\usage{
run_token_stats(state_dir)
}
\arguments{
\item{state_dir}{Directory of \verb{<task_id>.yaml} state files (e.g. the runner's
\code{.claude_task_runner/state}).}
}
\value{
A data frame: \code{task}, \code{attempt}, \code{output}, \code{input}, \code{cache_read},
\code{cost}. Its \code{summary} attribute holds median/mean/max per numeric column.
}
\description{
Parses the task-runner state YAMLs (one per task, each with a \code{runs} list
carrying \code{usage} and \code{cost_usd}) into a tidy per-run data frame, so the
measured cost economics (output and cache-read tokens, $/run) can be tracked.
}
