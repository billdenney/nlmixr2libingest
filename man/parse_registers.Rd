% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registers.R
\name{parse_registers}
\alias{parse_registers}
\title{Parse the nlmixr2lib canonical-name registers into a tidy data frame}
\usage{
parse_registers(dir = registers_dir())
}
\arguments{
\item{dir}{Directory holding the register markdown files. Defaults to
\code{\link[=registers_dir]{registers_dir()}}.}
}
\value{
A data frame with columns \code{kind}, \code{name}, \code{description}, \code{units},
\code{type}, \code{scope}, \code{reference_category}, \code{role}, \code{source_aliases},
\code{example_models}, \code{notes}, plus \code{text} (FTS document) and \code{id}.
}
\description{
Reads the covariate, compartment, and parameter registers shipped by
\code{nlmixr2lib} and returns one row per canonical entry. This is the
deterministic substrate for \code{\link[=build_register_index]{build_register_index()}} / \code{\link[=lookup_canonical]{lookup_canonical()}};
it does no validation (that stays in \code{nlmixr2lib::checkModelConventions()}).
}
