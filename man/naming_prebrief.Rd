% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prebrief.R
\name{naming_prebrief}
\alias{naming_prebrief}
\title{Build a model-specific naming pre-brief from a paper}
\usage{
naming_prebrief(
  paper,
  kinds = "covariate",
  backend = .llmBackend(),
  top_k = 3L,
  dir = registers_dir(),
  db_path = register_db_path()
)
}
\arguments{
\item{paper}{Paper text, or a path to a (trimmed) paper file.}

\item{kinds}{Which register kinds to resolve: any of \code{"covariate"},
\code{"parameter"}, \code{"compartment"}.}

\item{backend}{LLM backend for the candidate-augmenting distillation
(\code{"ollama"}/\code{"none"}/\code{"auto"}); \code{"none"} uses the deterministic scan only.}

\item{top_k}{Rows to consider per lookup (the best is used).}

\item{dir}{Register directory (defaults to \code{\link[=registers_dir]{registers_dir()}}).}

\item{db_path}{Lookup index location (defaults to \code{\link[=register_db_path]{register_db_path()}}).}
}
\value{
An \code{nli_prebrief} list: one entry per kind (\code{matched}/\code{unmatched}),
plus \code{backend} and \code{n} (matched count). Render with \code{\link[=render_prebrief]{render_prebrief()}}.
}
\description{
Resolves the covariates (and optionally parameters/compartments) a paper uses
to their canonical \code{nlmixr2lib} names up front, so the agent ingests one small
report instead of loading the register or looking terms up turn-by-turn.
Candidates come from a deterministic register scan plus, when a local LLM is
configured, \code{\link[=distill_paper]{distill_paper()}}; each is resolved with \code{\link[=lookup_canonical]{lookup_canonical()}}.
Unmatched candidates (possible new canonical names) are flagged. This is a
prior, not a gate: the agent still source-traces every value against the paper.
}
