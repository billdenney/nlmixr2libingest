% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{embed_text}
\alias{embed_text}
\title{Embed text with the local embedding model}
\usage{
embed_text(
  texts,
  model = .embedModel(),
  backend = .llmBackend(),
  batch = 64L,
  quiet = FALSE
)
}
\arguments{
\item{texts}{Character vector of documents to embed.}

\item{model}{Embedding model name (default \code{nomic-embed-text}).}

\item{backend}{\code{"ollama"}, \code{"none"}, or \code{"auto"} (default from options).}

\item{batch}{Texts to embed per request (batched via \verb{/api/embed}); falls
back to per-item embedding if the batch endpoint is unavailable.}

\item{quiet}{Suppress the progress bar.}
}
\value{
A numeric matrix with one row per input text (embedding dimension
columns). Rows that fail to embed are \code{NA}.
}
\description{
Embed text with the local embedding model
}
